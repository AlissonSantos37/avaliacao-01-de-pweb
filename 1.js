function calcularDelta(a, b, c) {
  const delta = (b ** 2) - (4 * a * c);
  deltaderetorno;
}

const a = 1;
const b = -5;
const c = 6;

const delta = calcularDelta(a, b, c);
console.log(Î(delta) = $delta);


